services:
  containmentci:
    build: .
    ports:
      - "8080:8080"
    environment:
      CONTAINMENTCI_SIGNING_KEY: change-me
    volumes:
      - containmentci-data:/app/.containmentci

volumes:
  containmentci-data:

