services:
  containmentci:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      CONTAINMENTCI_SIGNING_KEY: ${CONTAINMENTCI_SIGNING_KEY:?set a strong signing key}
    volumes:
      - containmentci-data:/app/.containmentci

volumes:
  containmentci-data:

